version: "2"

services:
    mysql:
        image: mysql:8.0
        ports:
          - 3306
        environment:
          MYSQL_ROOT_PASSWORD: ""
          MYSQL_DATABASE: "maxbansplus"
          MYSQL_ALLOW_EMPTY_PASSWORD: "yes"